import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "assumptions.app",
  description: "Generated by https://twitter.com/haseabamin",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="flex min-h-full w-full h-screen flex-col items-center">
        {children}
      </body>
    </html>
  );
}
